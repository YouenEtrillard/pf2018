export interface Appear {
  // properties to animate
  properties?: string;

  // duration of the animation
  duration?: string;

  // type of transition (ex: ease-out)
  transitionType?: string;

  // delay of the animation
  delay?: number;
}
