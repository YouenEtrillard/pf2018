import { ParallaxCoverDirective } from './parallax-cover.directive';

describe('ParallaxCoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxCoverDirective();
    expect(directive).toBeTruthy();
  });
});
